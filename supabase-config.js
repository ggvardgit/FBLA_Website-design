/**
 * Supabase configuration for backend auth and data persistence.
 * Add your project URL and anon key from the Supabase Dashboard.
 * If not configured, the app falls back to local-only authentication.
 */
window.SUPABASE_CONFIG = {
    url: '',        // e.g. 'https://xxxxx.supabase.co'
    anonKey: ''     // Your project's anon/public key
};
